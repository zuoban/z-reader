# Z Reader 工程路线图

本路线图把“高质量、高性能、高可靠”转化为可验证的持续改进，而不是一次性的功能清单。
范围以自部署的个人与家庭书架为主；功能扩展不能降低核心数据安全、阅读体验和可维护性。

## 工作原则

- **先测量，再优化**：性能改动必须附带可复现的基准或用户可感知的指标。
- **可恢复才算可靠**：备份要定期做恢复演练，不能只验证备份文件存在。
- **核心链路优先**：登录、上传、入库、阅读、进度同步、备份与恢复必须有自动化保护。
- **小步、可回滚地发布**：每个 PR 聚焦一个目的，附带验证结果和必要的文档更新。

## 发布质量标准

每次发布前都应满足以下标准：

| 维度 | 可验证标准 |
| --- | --- |
| 正确性 | CI 全绿；核心用户路径的单元、集成与 E2E 测试通过 |
| 安全性 | 无未处置的高危依赖或代码扫描告警；密钥不进入仓库或镜像 |
| 可靠性 | 已在支持版本上验证数据库迁移、备份校验和恢复流程 |
| 性能 | 相对于已记录基线，关键场景无未经说明的回归 |
| 可运维性 | 健康检查、指标、日志和回滚/恢复说明可用 |
| 开源协作 | README、变更记录、升级说明与 issue/PR 信息完整 |

## 近期里程碑

### M0：工程基线与协作门禁

- [x] 统一开发环境、测试命令和最低 Go 版本的文档。
- [x] 对 PR 启用依赖变更审查、后端测试与静态检查、前端 lint/build/unit/E2E/视觉回归。
- [x] 对 Go 与 TypeScript 启用 CodeQL 静态安全分析，并由 Dependabot 定期提出依赖更新。
- [x] 在仓库设置中保护 `main`：要求通过 PR 和所有 CI 检查、禁止强制推送。
- [x] 为核心模块生成覆盖率基线；后续只约束“改动不得降低核心模块覆盖率”，而非设定无意义的全仓阈值。
- [x] 建立语义化版本、`CHANGELOG.md` 和 release checklist。

### M1：数据可靠性与恢复演练

- [ ] 为数据库迁移、并发进度保存、上传中断、损坏电子书补充集成和 fuzz 测试。
- [x] 在 CI 验证“备份 → 校验 → 恢复 → `/readyz`”的最小闭环。
- [x] 确定并公布初始恢复目标：RPO 不超过 24 小时，RTO 不超过 30 分钟；根据部署实践调整。
- [ ] 至少每月完成一次异地备份恢复演练，并记录恢复耗时和发现的问题。

### M2：性能基线与预算

- [ ] 固化 1,000 本和 10,000 本书的书架数据集与 Go benchmark 结果。
- [ ] 增加压测场景：登录、书架加载、搜索、阅读进度保存、上传与 TTS 队列。
- [ ] 记录后端 P50/P95 延迟、吞吐、CPU 和内存；性能问题通过 `pprof` 定位后再优化。
- [ ] 记录前端 LCP、INP、CLS、首屏 JavaScript 体积和离线命中率，并为关键页面建立回归预算。
- [ ] 性能 PR 必须提供基准前后对比；回归超过 10% 时需说明原因并获得批准。

### M3：可观测性与生产运行

- [ ] 为 `/metrics` 建立示例 Prometheus/Grafana dashboard，覆盖请求、错误、延迟、限流、TTS 队列、备份与存储空间。
- [ ] 为错误率、就绪检查失败、备份失败、磁盘空间不足和异常延迟提供告警建议。
- [ ] 增加发布后的容器启动 smoke test，并验证健康检查、持久化卷和升级路径。
- [ ] 提供部署、升级、回滚、备份和故障排查 runbook。

### M4：开源项目成熟度

- [ ] 发布架构说明和 ADR（重要技术决策记录）。
- [ ] 维护公开路线图、`good first issue`、`help wanted` 和安全标签。
- [ ] 明确支持矩阵：浏览器、移动端、文件格式、部署方式与已验证书库规模。
- [ ] 为每个正式版本提供变更日志、升级说明、已知限制和可验证的镜像标签。

## 度量与复盘

每月一次复盘以下信号：CI 失败率和耗时、已知安全告警、恢复演练结果、关键性能基线、线上错误率以及未处理 issue 的年龄。指标出现退化时，优先安排可靠性或性能修复，而不是继续扩展功能。
